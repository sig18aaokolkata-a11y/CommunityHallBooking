const mongoose = require('mongoose');
const fs = require('fs');
const path = require('path');

// Hardcoded for local script usage, normally this should be in .env but for this task I will just use it directly or expect it to be handled.
// Actually next.js handles .env, manual scripts don't. I'll read from process.env if available, or just use the connection logic from dbConnect locally.
// For simplicity, I'll assume the user will not run this manually, but I will make an API route to trigger it, which is safer and easier.

// Wait, I can make a temporary API route to seed. simpler.
